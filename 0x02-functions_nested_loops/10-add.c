#include "main.h"
/**
 * add - the function to add values
 * @i: the variable holds a value in the main program
 * @j: the variable holds a value in the main program
 *
 * Return: the return value is the addition of the other values
 */

int add(int i, int j)
{
	int sum;

	sum = i + j;

	return (sum);
}
