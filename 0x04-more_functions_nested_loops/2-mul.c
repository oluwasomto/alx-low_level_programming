#include <stdio.h>
/**
 * mul - this function multiplies the variables
 *
 * @a: the first variable
 * @b: the second variable
 *
 * Return: always 0
 */

int mul(int a, int b)
{
	int cal;

	cal = a * b;
	return (cal);
}
